public enum Tag{
    Player,
    Breakable,
    Enemy,
    Floor,
    Events,
}

public enum WeaponType
{
    Pistol,
    Riffle,
    ShotGun,
}

public enum EventType
{
    None,
    Gravity,
}

public enum DirGravity
{
    Upwards = -1,
    DownWards = 1,
}